Got it—here’s a single, copy-paste prompt for Replit (Ghostwriter) to implement the upgrades.

---

**Paste this into Replit:**

```
You are refactoring my landing section called “Massive Compute Engine” to feel like a real, agentic AI system (not buzzwords). 
Stack: React + Tailwind (or vanilla React + CSS if Tailwind isn’t present). 
Create small, reusable components and keep styles dark/black with subtle gold accents. 
No external paid libs. If you need icons, use lucide-react (optional). 

### Goals
1) Add REAL, ACTIONABLE capability blocks (not fluff).
2) Add a live-feeling telemetry strip with dynamic counters.
3) Show transparent data sources + tech stack.
4) Subtle animated background to imply continuous scanning.
5) Clean, premium “AI x Bloomberg Terminal” vibe.

### Files & Structure
- components/AgenticComputeSection.jsx     (top-level wrapper)
- components/TelemetryStrip.jsx            (live counters)
- components/CapabilitiesGrid.jsx          (what the agent actually DOES)
- components/DataSources.jsx               (MLS/APIs list with badges)
- components/TechStack.jsx                 (stack chips)
- components/AnimatedBackdrop.jsx          (subtle lines/pulses animation)
- styles/agentic.css                       (only if Tailwind not available)

If the app is not React/Tailwind, adapt to the current framework but keep the structure.

### Copy & Content (use this exact text unless minor edits improve UX)

SECTION HEADING:
“Massive Compute Engine”

SUBHEAD (one line):
“Agent Kammer’s distributed AI continuously scans every market, cross-verifies public records, and predicts shifts—while you sleep.”

PILL ROW (4 pills):
- “Continuous Market Scanning”
- “Autonomous Buyer Profiling”
- “Instant Alert Processing”
- “Under-Market Signal Detection”

TELEMETRY STRIP (dynamic counters, update every 2–3s with easing):
- “Listings scanned this week” → start around 2,800,000 and tick up randomly by 50–300
- “Verified data sources” → fixed: 12
- “Match accuracy (last 30d)” → random between 94–97%
- “Opportunities flagged under market” → random 8–20/day; daily count; show “today”

CAPABILITIES GRID (6 cards, concise + concrete):
1) Price Anomaly Detector
   “Flags listings priced 8–15% below local trend using time-series comps + agent remarks.”
2) Neighborhood Risk Scanner
   “Surfaces building violations, zoning changes, flood/heat risk, and planned developments.”
3) Buyer Fit Engine
   “Learns your lifestyle + budget constraints via NLP to rank matches—not spam.”
4) Micro-Trend Forecaster
   “Tracks interest rates, DOM, walkscore deltas, and migration to predict near-term moves.”
5) Off-Market Signal Radar
   “Parses permits, expireds, and owner intent signals to spot pre-list opportunities.”
6) Agent Match Optimizer
   “Pairs you with top-performing local agents for your niche and price band.”

DATA SOURCES (badges; two rows, wrap):
“OneKey MLS”, “CRMLS (CA)”, “Zillow Bridge”, “Realtor.com”, “Redfin”, “PropertyShark”, “Public Records”, “Tax Assessments”, “Valuations”, “Google Maps”, “School & Crime DBs”, “Walkscore”

TECH STACK (chips):
“OpenAI”, “LangChain”, “MongoDB”, “Cloudflare Workers”, “Zillow/MLS APIs”, “PostgreSQL”, “Node/Express”, “Python”, “Redis”, “Airflow”, “Vector Search”, “S3/Cloud Storage”

FOOTER METRIC (side-by-side):
Left: “240hrs” label “Average buyer search time”
Right: “240+ hrs saved” label “With Agent Kammer”

### Visual & Motion Requirements
- Background: subtle animated “data lines” + soft pulsing nodes (CSS keyframes). Very low opacity; no distraction.
- Section card: black (#0B0B0C) with soft borders (rgba gold 1px) and gentle glow on hover.
- Counters: mono or tabular nums; smooth ease-out transitions.
- Use prefers-reduced-motion media query to disable animations.

### Implementation Notes
- Telemetry counters: implement with setInterval inside useEffect; clear on unmount. Keep deterministic min/max so UX is believable.
- If Tailwind: use utilities like text-zinc-200, bg-black, border-zinc-800, ring-1 ring-yellow-500/20 for accents.
- Accessibility: proper headings, aria-live="polite" on telemetry values, sufficient contrast.
- Mobile: stack cards 1-col; on desktop 3-col.
- Do not block main thread with heavy timers; 2–3s intervals; random jitter.
- All numbers should be easily configurable at the top of the component.

### Acceptance Criteria
- Section renders with animated backdrop, headline + subhead, pill row, telemetry strip, capabilities grid, data sources, tech stack, and the 240h savings pair.
- Counters animate realistically and stop when component unmounts.
- No third-party heavy chart libs introduced.
- Dark, premium, minimal; no buzzword filler; all features read as concrete benefits.

### Starter Skeleton (if helpful)
Create AgenticComputeSection.jsx that imports and composes:
<AnimatedBackdrop />
<h2>Massive Compute Engine</h2>
<Pills />
<TelemetryStrip />
<CapabilitiesGrid />
<DataSources />
<TechStack />
<FooterMetrics />

Export and mount it where the current “Massive Compute Engine” section lives. 
Refactor existing styles to match this spec.
```

---

If you want, I can also give you a ready React/Tailwind component to drop in—but this prompt alone should guide Ghostwriter to build the whole thing exactly how we outlined.
