# Decision Companion Components

This folder is for the persistent Decision Companion: conversation, profile state, progress, navigation recommendations, and save/schedule actions.

The current implementation lives in `client/src/components/DecisionAssistantDock.tsx`. Split it into this folder once the interaction model stabilizes.
